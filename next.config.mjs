/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['d3i7mz61kz7ozx.cloudfront.net'], 
        domains: ['via.placeholder.com'], 

 
      },

};

export default nextConfig;
